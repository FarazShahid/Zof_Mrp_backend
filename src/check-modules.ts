// This is a simple script to check if the modules are installed correctly
import { ApiProperty } from '@nestjs/swagger';
import { Type } from 'class-transformer';

console.log('Modules are installed correctly!'); 